# M93 Decision - 审计时间线视图

## 决策
采用只读聚合页展示审计时间线，不在面板里新增任何执行或修复操作。

## 原因
审计时间线是解释系统行为的证据层。它必须忠实展示已有 trace/evidence，而不是成为新的动作入口。

## 结果
- 用户能看到任务事件顺序和证据摘要。
- 所有动作仍必须通过既有 PermissionGate 或后续授权流程。
- M93 不改变审计写入格式。
