# M146 Decision - 补丁审查液态玻璃页

## 决策

把补丁预览和批准写入边界做成独立产品页，而不是只放在工程面板中。

## 背景

Biot 的写入安全依赖用户理解 diff 和风险。补丁审查页需要让用户看到“将要改什么、风险是什么、什么时候可以写入”。

## 结果

- 补丁审查页展示多文件 diff、风险摘要、批准写入状态。
- 页面只表达门禁，不新增 bypass 或自动 apply 行为。
- 继续使用液态玻璃组件体系。

## 风险

- 当前仍是产品化总览，真实 diff 内容可在后续接入已有 PatchPreviewPanel 数据。
