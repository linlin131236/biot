# M145 Decision - 权限中心液态玻璃页

## 决策

把权限中心作为设置中心的独立分类，而不是继续埋在常规设置中。

## 背景

Biot 的核心差异是安全、权限、审计和人工批准。设置中心必须让用户一眼看到权限边界，而不是只在首页提示。

## 结果

- 权限中心展示待批准请求、写入门禁和审计记录。
- 页面只读，不新增自动批准或自动执行入口。
- 用户能理解写入、apply 和恢复都必须等待确认。

## 风险

- 当前展示的是产品化状态面板，尚未接真实权限队列详情；后续可接已有权限 API。
