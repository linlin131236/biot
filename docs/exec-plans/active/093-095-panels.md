# M93-M95 Exec Plans — 审计时间线 + 诊断中心 + 发布准备页

## M93 审计时间线视图
- 后端：新增 `/audit-timeline` 聚合端点（可选 closure_id 过滤）
- 前端：`AuditTimelinePanel.tsx`，展示 queue→handoff→permission→closure→evidence 全过程
- 验收：时间线排序、中文状态/来源、证据引用路径、无执行按钮

## M94 诊断中心
- 后端：新增 `/diagnostics-center` 聚合端点（合并 diagnostics + integrity）
- 前端：`DiagnosticsCenterPanel.tsx`，统一展示阻断/警告/提示
- 验收：中文原因/影响/建议、区分"必须修复"和"可接受风险"、不自动修复

## M95 发布准备页
- 后端：复用已有 API（release-readiness + local-release-checklist + recovery-policy）
- 前端：`ReleaseReadinessPanel.tsx`，显示 ready/not ready + 检查清单
- 验收：只读、显示阻断项、无 release/tag/push/delete 按钮
