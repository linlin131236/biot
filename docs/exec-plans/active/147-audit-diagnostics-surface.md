# M147 Exec Plan - 审计诊断液态玻璃页

## 目标

在设置中心新增审计诊断页，集中展示审计时间线、诊断中心和恢复建议。

## 范围

- 新增“审计诊断”导航项。
- 展示审计时间线、诊断中心、恢复建议。
- 只显示下一步建议，不自动执行修复。

## 验收

- 点击“审计诊断”显示审计诊断标题。
- 页面显示审计时间线、诊断中心、恢复建议。
- 页面说明阻断、警告、提示和下一步建议按优先级展示。
- 目标测试通过。
